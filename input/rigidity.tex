\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{xfrac} % For \sfrac command
\usepackage{amsmath}
\usepackage{amssymb}

\title{rigidity}
\author{haiml76 }
\date{February 2026}

\begin{document}

\maketitle

\section{Criteria for rigidity}
Let $k=\mathbb{Q}_p$, and let $L=\mathcal{L}_{n,p}$ be our usual algebra over $\mathbb{Q}_p$.\\
\subsection{The $\mathcal{X}$ criterion}
Let $\mathcal{X}:=\{x\in{L}\setminus\gamma_2L : C_{\sfrac{L}{\gamma_3}}(x)=\mathbb{Q}_px+\gamma_2L\}$, we need to verify that $\mathcal{X}$ generates the entire $L$.\\
Let $x\in{L}\setminus\gamma_2{L}$, that is\\  \[x=a_1e_{1,2}+a_2e_{2,3}+\cdots+a_{n-1}e_{n-1,n}+\sum_{r=2}^{n-1}\sum_{i=1}^{n-r}a_{i,i+r}e_{i,i+r},\] 
where $a_1,a_2,\dots,a_{n-1}$ are not all zero.\\
Let $y\in{C}_{\sfrac{L}{\gamma_3}}(x)$, that is $y=\sum_{r=1}^{n-1}\sum_{i=1}^{i-r}b_{i,i+r}e_{i,i+r}$ such that $[x,y]\in\gamma_3L$.\\
But $[b_{i,i+r}e_{i,i+r},x]\in\gamma_3L$, for every $2\leq{r}\leq{n-1}$ and $1\leq{i}\leq{n-r}$. Thus
\[
[x,y]=\sum_{i=1}^{n-1}\left(a_ib_{i+1}-a_{i+1}b_i\right)[e_{i,i+1},e_{i+1,i+2}]+\gamma_3L=\sum_{i=1}^{n-1}\left(a_ib_{i+1}-a_{i+1}b_i\right)e_{i,i+2}+\gamma_3L
,\]
hence $[x,y]\in\gamma_3L\iff{a}_ib_{i+1}-a_{i+1}b_i=0$, for every $1\leq{i}\leq{n-1}$.\\
But \[a_{i}b_{i+1}-a_{i+1}b_i=0\iff\det\begin{pmatrix}
a_i&a_{i+1}\\
b_i&b_{i+1}
\end{pmatrix}=0\iff\begin{pmatrix}
    b_i&b_{i+1}
\end{pmatrix}=c\begin{pmatrix}
    a_i&a_{i+1}
\end{pmatrix},\]
for some $c\in\mathbb{Q}_p$.\\
Assume $a_i\neq{0}$ for every $1\leq{i}\leq{n-1}$, then 
\begin{align*}
&a_1b_2-a_2b_1=0&\iff&\begin{pmatrix}
    b_1&b_2
\end{pmatrix}=c_1\begin{pmatrix}
    a_1&a_2
\end{pmatrix}.\\
&a_2b_3-a_3b_2=0&\iff&\begin{pmatrix}
    b_2&b_3
\end{pmatrix}=c_2\begin{pmatrix}
    a_2&a_3
\end{pmatrix}.\\
&\vdots&\vdots\\
&a_{n-1}b_n-a_nb_{n-1}=0&\iff&\begin{pmatrix}
    b_{n-1}&b_n
\end{pmatrix}=c_{n-1}\begin{pmatrix}
    a_{n-1}&a_n
\end{pmatrix}.
\end{align*}
But $b_{i+1}=c_ia_{i+1}=c_{i+1}a_{i+1}$ for every $1\leq{i}\leq{n-1}$, hence \\
$c_1=c_2=\cdots=c_{n-1}=c$, which implies that $y=cx+\gamma_2L$ for some $c\in\mathbb{Q}_p$, but this is true for every $c\in\mathbb{Q}_p$, which means that $y\in\mathbb{Q}_px+\gamma_2L$ for every $x\in{L}$, but we assumed that $a_i\neq{0}$ for every $1\leq{i}\leq{n-1}$, hence $x\in\mathcal{X}$ has all the elements $e_{i,i+1}$ for $1\leq{i}\leq{n-1}$, thus it generates the entire algebra $L$.\\ 
Assume $a_i=0$ for some $2\leq{i}\leq{n-2}$, and assume that $a_j\neq{0}$ for every $j\neq{i}$, then $a_ib_{i+1}-a_{i+1}b_i=-a_{i+1}b_i=0$ and $a_{i-1}b_i-a_ib_{i-1}=a_{i-1}b_i=0$ if and only if $b_i=0$. But then $\frac{b_1}{a_1}=\frac{b_2}{a_2}=\cdots=\frac{b_{i-1}}{a_{i-1}}=c_1$ and $\frac{b_{i+1}}{a_{i+1}}=\frac{b_{i+2}}{a_{i+2}}=\cdots=\frac{b_{n-1}}{a_{n-1}}=c_2$, for $c_1,c_2\in\mathbb{Q}_p$, hence $y=c_1(a_1e_{1,2}+a_2e_{2,3}+\cdots+a_{n-1}e_{i-1,i})+c_2(a_{i+1}e_{i+1,i+2}+a_{i+2}e_{i+2,i+3}+\cdots+a_{n-1}e_{n-1,n})+\gamma_2L\notin\mathbb{Q}_px+\gamma_2L\implies{x}\notin\mathcal{X}$, which is an example for an element whose centralizers are not necessarily in $\mathbb{Q}_px+\gamma_2L$.
\subsection{The $\mathcal{Y}$ criterion}
For $n=5$.\\
Let $x\in{L}\setminus\gamma_2L$, and assume that $a_i\neq{0}$ for every $1\leq{i}\leq{4}$.\\
Let $y\in{C}_L(x)$, we already know that $b_i=ca_i$, for some $c\in\mathbb{Q}_p$. We look at $(a_1b_{24}-a_{24}b_1+a_{13}b_3-a_3b_{13})e_{14}=\\
=(a_1b_{24}-a_{24}ca_1+a_{13}ca_3-a_3b_{13})e_{14}=0$.\\
Assume $a_{24}\neq{0}$ and denote $d=\frac{b_{24}}{a_{24}}$, then $a_1(b_{24}-ca_{24})+a_3(ca_{13}-b_{13})=\\a_1a_{24}(d-c)+a_3(ca_{13}-b_{13})=0\implies{b}_{13}=\frac{a_1a_{24}
(d-c)}{a_3}+ca_{13}$.\\
Also, $(a_2b_{35}-a_{35}b_2+a_{24}b_4-a_4b_{24})e_{25}=\\
=(a_2b_{35}-a_{35}ca_2+a_{24}ca_4-a_4da_{24})e_{25}=0\implies$\\$\implies{b}_{35}=\frac{a_{24}a_4(d-c)}{a_2}+ca_{35}$.\\
Hence, $b_{13}=ca_{13}$ and $b_{35}=ca_{35}$ if and only if $d=c\iff{b}_{24}=ca_{24}$.
Now we look at $(a_1b_{25}-a_{25}b_1+a_{13}b_{35}-a_{35}b_{13}+a_{14}b_4-a_4b_{14})e_{15}=\\
=(a_1b_{25}-a_{25}ca_1+a_{13}(\frac{a_{24}a_4(d-c)}{a_2}+ca_{35})-a_{35}(\frac{a_1a_{24}(d-c)}{a_3}+ca_{13})+ca_{14}a_4-a_4b_{14})e_{15}=0$.\\
Assume $a_{14}\neq{0}$ and denote $r=\frac{b_{14}}{a_{14}}$, then $b_{25}=\frac{1}{a_1}(ca_1a_{25}-a_{13}a_3a_{24}a_4(d-c)+\\+a_1a_{24}a_2a_{35}(d-c)-a_{14}a_4(c-r))$.\\
Thus, for an arbitrary element $x\in{L}\setminus\gamma_2$, any $y\in{C}_L$ must be of the form
\begin{align*}
&y=a_1e_{12}+a_2e_{23}+a_3e_{34}+a_4e_{45}+\\
&+\left[\frac{a_1a_{24}(d-c)}{a_3}+ca_{13}\right]e_{13}+da_{24}e_{24}+\\
&+\left[\frac{a_4a_{24}(d-c)}{a_2}+ca_{35}\right]e_{35}+ra_{14}e_{14}+\\
&+\left[ca_{25}-\frac{a_{13}a_3a_{24}a_4(d-c)}{a_1}+a_{24}a_2a_{35}(d-c)-\frac{a_{14}a_4(c-r)}{a_1}\right]e_{25}.
\end{align*}
Let $z\in{C}_L(y)$. We assumed that $a_i\neq{0}$ for every $1\leq{i}\leq{n-1}$, which implied that $b_i=ca_i$, but $b_iz_{i+1}-b_{i+1}z_i=c(a_iz_{i+1}-a_{i+1}z_i)=0$ hence there exists $\tilde{c}\in\mathbb{Q}_p$ such that $\begin{pmatrix}
    z_1&z_2&\dots&z_{n-1}
\end{pmatrix}=\tilde{c}\begin{pmatrix}
    a_1&a_2&\dots&a_{n-1}
\end{pmatrix}$.\\
Now we look at $(b_1z_{24}-b_{24}z_2+b_{13}z_3-b_3z_{13})e_{14}=\\
=\left[ca_1z_{24}-da_{24}\tilde{c}a_1+\left(\frac{a_1a_{24}(d-c)}{a_3}+ca_{13}\right)\tilde{c}a_3-ca_3z_{13}\right]e_{14}=0$.\\
We assumed $a_{24}\neq{0}$, so we denote $\tilde{d}=\frac{z_{24}}{a_{24}}$, then
\begin{align*}
&z_{13}=\frac{1}{ca_3}\left[a_1a_{24}(c\tilde{d}-\tilde{c}d)+(a_1a_{24}(d-c)+ca_{13}a_3)\tilde{c}\right]=\\
&=\frac{1}{ca_3}\left[a_1a_{24}(c\tilde{d}-\tilde{c}d+\tilde{c}d-c\tilde{c})+c\tilde{c}a_{13}a_3\right]=\frac{1}{ca_3}\left[a_1a_{24}(c\tilde{d}-c\tilde{c})+c\tilde{c}a_{13}a_3\right]=\\
&=\frac{1}{ca_3}\left[ca_1a_{24}(\tilde{d}-\tilde{c})+c\tilde{c}a_{13}a_3\right]=\frac{1}{a_3}\left[a_1a_{24}(\tilde{d}-\tilde{c})+\tilde{c}a_{13}a_3\right],
\end{align*} 
and $z_{35}=\frac{1}{a_2}\left[a_4a_{24}(\tilde{d}-\tilde{c})+\tilde{c}a_2a_{35}\right]$,\\
and it is readily seen that $z_{13}=\tilde{c}a_{13}$ and $z_{35}=\tilde{c}a_{35}$ if and only if $\tilde{d}=\tilde{c}$, which implies that for arbitrary values of $\tilde{c},\tilde{d}$, the centralizer element $z\notin\mathbb{Q}_px+Z(L)$.
@@@@@@@@@@@@@@@
Now we look at $(b_1z_{25}-b_{2,5}z_1+b_{1,3}z_{3,5}-b_{3,5}z_{1,3}+b_{1,4}z_4-b_4z_{1,4})e_{1,5}=\\
=(ca_1z_{2,5}-\left[\frac{1}{a_1}(ca_1a_{2,5}-a_{1,3}a_3a_{2,4}a_4(d-c)+a_1a_{2,4}a_2a_{3,5}(d-c)-a_{1,4}a_4(c-r)\right]\tilde{c}a_1+\frac{1}{a_2}\left[\frac{a_1a_{2,4}
(d-c)}{a_3}+ca_{1,3}\right]\left[a_{2,4}a_4(\tilde{d}-\tilde{c})+\tilde{c}a_2a_{3,5}\right]-\frac{1}{a_3}\left[\frac{a_{2,4}a_4(d-c)}{a_2}+ca_{3,5}\right]\left[a_1a_{2,4}(\tilde{d}-\tilde{c})+\tilde{c}a_{1,3}a_3\right]+ra_{1,4}\tilde{c}a_4-ca_4z_{1,4}$.\\
Denote $\tilde{r}=\frac{z_{1,4}}{a_{1,4}}$, then $z_{2,5}=\frac{1}{a_1}(ca_1a_{2,5}-a_{1,3}a_3a_{2,4}a_4(d-c)+\\+a_1a_{2,4}a_2a_{3,5}(d-c)-a_{1,4}a_4(c-r))$.\\
It is readily seen that is $r=d=c$ then






and it is readily seen that if $\tilde{d}=\tilde{c}$ then $z_{1,3}=\tilde{c}a_{1,3}$ and $z_{3,5}=\tilde{c}a_{3,5}$, but if $\tilde{d}\neq\tilde{c}$ then $z_{1,3}$ and $z_{3,5}$ are not necessarily of this form, hence $x\notin\mathcal{Y}$ in this case, which shows that an arbitrary $x\in{L}\setminus\gamma_2L$ is not necessarily in $\mathcal{Y}$.
\end{document}
