\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{xfrac} % For \sfrac command
\usepackage{amsmath}
\usepackage{amssymb}

\title{rigidity}
\author{haiml76 }
\date{February 2026}

\begin{document}

\maketitle

\section{Criteria for rigidity}
Let $k=\mathbb{Q}_p$, and let $L=\mathcal{L}_{n,p}$ be our usual algebra over $\mathbb{Q}_p$.\\
\subsection{The $\mathcal{X}$ criterion}
Let $\mathcal{X}:=\{x\in{L}\setminus\gamma_2L : C_{\sfrac{L}{\gamma_3}}(x)=\mathbb{Q}_px+\gamma_2L\}$, we need to verify that $\mathcal{X}$ generates the entire $L$.\\
Let $x\in{L}\setminus\gamma_2{L}$, that is\\  \[x=a_1e_{1,2}+a_2e_{2,3}+\cdots+a_{n-1}e_{n-1,n}+\sum_{r=2}^{n-1}\sum_{i=1}^{n-r}a_{i,i+r}e_{i,i+r},\] 
where $a_1,a_2,\dots,a_{n-1}$ are not all zero.\\
Let $y\in{C}_{\sfrac{L}{\gamma_3}}(x)$, that is $y=\sum_{r=1}^{n-1}\sum_{i=1}^{i-r}b_{i,i+r}e_{i,i+r}$ such that $[x,y]\in\gamma_3L$.\\
But $[b_{i,i+r}e_{i,i+r},x]\in\gamma_3L$, for every $2\leq{r}\leq{n-1}$ and $1\leq{i}\leq{n-r}$. Thus
\[
[x,y]=\sum_{i=1}^{n-1}\left(a_ib_{i+1}-a_{i+1}b_i\right)[e_{i,i+1},e_{i+1,i+2}]+\gamma_3L=\sum_{i=1}^{n-1}\left(a_ib_{i+1}-a_{i+1}b_i\right)e_{i,i+2}+\gamma_3L
,\]
hence $[x,y]\in\gamma_3L\iff{a}_ib_{i+1}-a_{i+1}b_i=0$, for every $1\leq{i}\leq{n-1}$.\\
But \[a_{i}b_{i+1}-a_{i+1}b_i=0\iff\det\begin{pmatrix}
a_i&a_{i+1}\\
b_i&b_{i+1}
\end{pmatrix}=0\iff\begin{pmatrix}
    b_i&b_{i+1}
\end{pmatrix}=c\begin{pmatrix}
    a_i&a_{i+1}
\end{pmatrix},\]
for some $c\in\mathbb{Q}_p$.\\
Assume $a_i\neq{0}$ for every $1\leq{i}\leq{n-1}$, then 
\begin{align*}
&a_1b_2-a_2b_1=0&\iff&\begin{pmatrix}
    b_1&b_2
\end{pmatrix}=c_1\begin{pmatrix}
    a_1&a_2
\end{pmatrix}.\\
&a_2b_3-a_3b_2=0&\iff&\begin{pmatrix}
    b_2&b_3
\end{pmatrix}=c_2\begin{pmatrix}
    a_2&a_3
\end{pmatrix}.\\
&\vdots&\vdots\\
&a_{n-1}b_n-a_nb_{n-1}=0&\iff&\begin{pmatrix}
    b_{n-1}&b_n
\end{pmatrix}=c_{n-1}\begin{pmatrix}
    a_{n-1}&a_n
\end{pmatrix}.
\end{align*}
But $b_{i+1}=c_ia_{i+1}=c_{i+1}a_{i+1}$ for every $1\leq{i}\leq{n-1}$, hence \\
$c_1=c_2=\cdots=c_{n-1}=c$, which implies that $y=cx+\gamma_2L$ for some $c\in\mathbb{Q}_p$, but this is true for every $c\in\mathbb{Q}_p$, which means that $y=\mathbb{Q}_px+\gamma_2L$ for every $x\in{L}$, but we assumed that $a_i\neq{0}$ for every $1\leq{i}\leq{n-1}$, hence $x\in\mathcal{X}$ has all the elements $e_{i,i+1}$ for $1\leq{i}\leq{n-1}$, thus it generates the entire algebra $L$.\\ 
Assume $a_i=0$ for some $2\leq{i}\leq{n-2}$, and assume that $a_j\neq{0}$ for every $j\neq{i}$, then $a_ib_{i+1}-a_{i+1}b_i=-a_{i+1}b_i=0$ and $a_{i-1}b_i-a_ib_{i-1}=a_{i-1}b_i=0$ if and only if $b_i=0$. But then $\frac{b_1}{a_1}=\frac{b_2}{a_2}=\cdots=\frac{b_{i-1}}{a_{i-1}}=c_1$ and $\frac{b_{i+1}}{a_{i+1}}=\frac{b_{i+2}}{a_{i+2}}=\cdots=\frac{b_{n-1}}{a_{n-1}}=c_2$, for $c_1,c_2\in\mathbb{Q}_p$, hence $y=c_1(a_1e_{1,2}+a_2e_{2,3}+\cdots+a_{n-1}e_{i-1,i})+c_2(a_{i+1}e_{i+1,i+2}+a_{i+2}e_{i+2,i+3}+\cdots+a_{n-1}e_{n-1,n})+\gamma_2L\notin\mathbb{Q}_px+\gamma_2L\implies{x}\notin\mathcal{X}$, which is an example for an element whose centralizers are not necessarily in $\mathcal{X}$.
\subsection{The $\mathcal{Y}$ criterion}
For $n=5$.\\
Let $x\in{L}\setminus\gamma_2L$, that is $x=a_1e_{1,2}+a_2e_{2,3}+a_3e_{3,4}+a_4e_{4,5}$, and $x\in{C}_L(x)$, that is $y=b_{1,2}e_{1,2}+b_{2,3}e_{2,3}+\cdots+b_{1,5}e_{1,5}$.

We already know that \[a_1b_{2,3}e_{1,3}=a_2b_{3,4}e_{2,4}=a_3b_{4,5}e_{3,5}=0\iff\begin{pmatrix}
    b_{1,2}&b_{2,3}&b_{3,4}&b_{4,5}
\end{pmatrix}=c\begin{pmatrix}
    a_1&a_2&a_3&a_4
\end{pmatrix},\]
for some $c\in\mathbb{Q}_p$.
We also have the constraint\\ 
$(a_1b_{2,4}-a_3b_{1,3})e_{1,4}=(a_2b_{3,5}-a_4b_{2,4})e_{2,5}=0$, but then\\
\[\begin{pmatrix}
    b_{1,3}&b_{2,4}
\end{pmatrix}=\beta\begin{pmatrix}
    a_1&a_3
\end{pmatrix}\]
and
\[\begin{pmatrix}
    b_{2,4}&b_{3,5}
\end{pmatrix}=\alpha\begin{pmatrix}
    a_2&a_4
\end{pmatrix},\]
for some $\alpha,\beta\in\mathbb{Q}_p$.\\
But then $b_{2,4}=\beta{a}_3=\alpha{a}_2$, which implies that $\alpha\neq\beta\iff{a_2}\neq{a_3}$, hence
\[\begin{pmatrix}
    b_{1,3}&b_{2,4}&b_{3,5}
\end{pmatrix}=\begin{pmatrix}
    \beta{a}_1&\beta{a}_2&\alpha{a_4}
\end{pmatrix}=\begin{pmatrix}
    \beta{a}_1&\beta{a}_2&\beta\frac{a_3}{a_2}a_4
\end{pmatrix}.\]
We also have the constraint 
\[(a_1b_{2,5}-a_4b_{1,4})e_{1,5}=0\iff\begin{pmatrix}
    b_{1,4}&b_{2,5}
\end{pmatrix}=\gamma\begin{pmatrix}
    a_1&a_4
\end{pmatrix},\]
for some $\gamma\in\mathbb{Q}_p$.
Hence, \[y=cx+\beta({a}_1e_{1,3}+{a}_2e_{2,4}+\frac{a_3a_4}{a_2}e_{3,5})+\gamma(a_1e_{1,4}+a_4e_{2,5})+b_{1,5}e_{1,5}.
\]

Let $\mathcal{Y}:=\{x\in{L}\setminus\gamma_2L : C_L(C_L(x))={k}x+Z(L)\}$, we need to verify that $\mathcal{Y}=L\setminus\gamma_2L$.\\
Let $x\in{L}\setminus\gamma_2{L}$, that is $x=a_1e_{1,2}+a_2e_{2,3}+\cdots+a_{n-1}e_{n-1,n}$, and let $y\in{C_L(x)}$, that is $y=b_{1,2}e_{1,2}+b_{2,3}e_{2,3}+\cdots+b_{n-1,n}e_{n-1,n}+b_{1.3}e_{1,3}+b_{2.4}e_{2,4}+\cdots+b_{1,n-1}e_{1,n-1}+b_{2,n}e_{2,n}$, such that $[x,y]=0$, but then every expression of the form $a_ib_{i+1,i+1+r}-a_{i+r}b_{i,i+r}$, for $1\leq{r}\leq{n-2}$ and $1\leq{i}\leq{n-(r+1)}$, must vanish. but\\
\[a_{i}b_{i+1,i+1+r}=a_{i+r}b_{i,i+r}\iff\det\begin{pmatrix}
a_i&a_{i+r}\\
b_{i,i+r}&b_{i+1,i+1+r}
\end{pmatrix}=0\iff\]\[\iff{b_{i,i+r}}e_{i,i+r}+b_{i+1,i+1+r}e_{i+1,i+1+r}=c_r(a_ie_{i,i+1}+a_{i+r,i+r+1}e_{i+r,i+r+1}),\]
for some $c_r\in\mathbb{Q}_p$. Hence, $0$
\end{document}
